
type FirebaseErrorsType = Record<string, string>

export const FirebaseErrors: FirebaseErrorsType = {
    "auth/invalid-credential": "Email or password is invalid.",
    "auth/email-already-in-use": "Email is already in use.",
}